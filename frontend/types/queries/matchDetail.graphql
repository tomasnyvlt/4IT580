query matchDetail($id: Int!) {
    match(id_match: $id) {
        id_match
        time_created
        time_start
        state
        edit_hash
        id_league
        teams {
            id_team
            name
        }
        score {
            name
            points
        }
    }
}
